pipeline {
    agent any
    tools { nodejs "nodejs" }

	// Change each stage to what you intend to do
    stages {
        stage('Tools initiated') {
            steps {
                // initiate the cypress tool
            }
        }
        stage('Checkout code') {
            steps {
                // checkout if the tool can get code form github
            }
        }
        stage('Build') {
            steps {
                // see if the build is able to run  
            }
        }
        stage('Unit Test') {
            steps {
                // run a unit test where it goes into an item and checks if it is possible to click the item and add it to the shopping basket
            }
        }
        stage('Start System') {
            steps {
                // Startup system
            }
        }
        stage('automated system tests') {
            steps {
                // Run automated system tests
            }
        }
    }
}
